# Pregenerated plots for WARio

These are the results for easy access without building and running the benchmarks
When running the scripts, the generated results will appear in the `plot/` directory instead
